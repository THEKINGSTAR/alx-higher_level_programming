#!/usr/bin/node

const nfound = 'No argument';
const args = process.argv;

let i = 2;
while (args[i] !== undefined) {
  console.log(args[i]);
  i++;
}
if (args[2] === undefined) {
  console.log(nfound);
}
